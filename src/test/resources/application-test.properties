# --- App ---
spring.application.name=api

# --- Database (H2 for tests) ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# --- Redis (disabled for tests) ---
spring.data.redis.repositories.enabled=false
spring.cache.type=none

# --- JWT (test only) ---
jwt.secret=cc0724944687a168de6429d3e86de951b7fa3804d83555d6e88c2ebfc2b61a36

# --- Liquibase ---
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml

# --- Logging ---
logging.level.org.springframework.security=DEBUG
logging.level.com.veche.api.security=DEBUG
logging.level.com.veche.api.event.VotingSessionStatusListener=INFO
logging.level.liquibase=WARN
logging.level.org.hibernate=WARN

# --- Security ---
server.ssl.enabled=false

# --- AI (mocked for tests) ---
spring.ai.openai.api-key=test